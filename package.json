{
  "name": "kafka",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc -b && node dist/index.js",
    "produce":"tsc -b && node dist/producer.js",
    "consume":"tsc -b && node dist/consumer.js",
    "producer_partitions":"tsc -b && node dist/producer_partitions.js",
    "consumer_partitions":"tsc -b && node dist/consumer_partitions.js" 
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "typescript": "^5.5.3"
  }
}
